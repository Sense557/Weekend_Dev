function exec () {
    console.log("Timer Done");
    
}

setTimeout (exec, 3000);

/*
    - Which one of the above function is a HOF ? // setTimeout
    - Which one of the above function is a callback ? // exec
*/